all:
	go install ./...

get_dev:
	go get -t ./...

test:
	go test -race -cover -covermode=atomic ./... 

bench:
	go test -bench . -benchmem ./...

lint:
	golangci-lint run --config=.golangci.yml ./...
